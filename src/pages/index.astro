---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<!doctype html>
<html lang="es">
    <head>
        <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
        <style>
            /* --- ESTILOS PARA EL EFECTO --- */
            .parallax-wrapper {
                position: relative;
                height: 60vh; /* Altura de la sección */
                display: flex;
                flex-direction: column;
                align-items: center;
                justify-content: center;
                overflow: hidden; /* Para que no se salgan las capas */
                background: #13151a; /* Un color oscuro profesional */
                color: white;
                text-align: center;
            }

            .parallax-bg {
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 120%; /* Más alta para tener margen de movimiento */
                background-image: url('https://images.unsplash.com/photo-1477346611705-65d1883cee1e?auto=format&fit=crop&q=80&w=2070');
                background-size: cover;
                background-position: center;
                z-index: 1;
                opacity: 0.4;
                transform: translateY(0);
                will-change: transform;
            }

            .content {
                position: relative;
                z-index: 2; /* Por encima del fondo */
                padding: 2rem;
            }

            h1 {
    			font-size: 4rem;
   				margin-bottom: 1rem;
    			text-shadow: 2px 2px 10px rgba(0,0,0,0.3); /* Sombra suave para separar del fondo */
    			color: #F5F5DC; /* <--- Color Crema/Beige */
			}

            main {
                max-width: 800px;
                margin: 0 auto;
                padding: 3rem 1rem;
            }
        </style>
    </head>
    <body>
        <Header />
        
        <section class="parallax-wrapper">
            <div class="parallax-bg" id="bg"></div>
            <div class="content" id="text">
                <h1> ¡Hola! Soy Leandro</h1>
                <p style="font-size: 1.5rem;">Bienvenidos a mi espacio personal.</p>
            </div>
        </section>

        <main>
            <p>
                Por aquí dejaré constancia de todos los proyectos que llevo a cabo en mi día a día, así como
                algunas cosillas interesantes que no quiero que se olviden, ya sean personalizaciones concretas o enlaces útiles.
            </p>
            <p>
                Poco a poco iré añadiendo todo, ¡a disfrutar!
            </p>
            
            <div style="height: 100vh;"></div>
        </main>

        <Footer />

        <script>
            window.addEventListener('scroll', () => {
                const scrollValue = window.scrollY;
                const bg = document.getElementById('bg');
                const text = document.getElementById('text');

                // Movemos el fondo a un 50% de la velocidad del scroll
                if(bg) bg.style.transform = `translateY(${scrollValue * 0.5}px)`;
                
                // Movemos el texto un poquito hacia arriba (efecto sutil)
                if(text) text.style.transform = `translateY(${scrollValue * -0.1}px)`;
            });
        </script>
    </body>
</html>